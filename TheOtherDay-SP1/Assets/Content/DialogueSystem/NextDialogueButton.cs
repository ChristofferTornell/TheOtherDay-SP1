using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class NextDialogueButton : MonoBehaviour
{
    /*
    //public DialogueBox dialogueBoxUI;
    public Dialogue currentDialogue;
    void Start()
    {
        Button btn = GetComponent<Button>();
        btn.onClick.AddListener(GoToNextDialogue);
    }

    public void UpdateDialogue()
    {
        currentDialogue = DialogueManager.instance.currentDialogue;
    }

    public void OnMouseDown()
    {

    }
    public void GoToNextDialogue()
    {
        Debug.Log("Pressed button to go to next Dialogue");
        DialogueManager.instance.currentDialogue = DialogueManager.instance.dialogueBoxUI.currentDialogue.nextDialogue;
        DialogueManager.instance.dialogueBoxUI.TakeNewDialogue();
    }
    */
}
